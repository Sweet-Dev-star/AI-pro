{
  "name": "polarity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Enter a thesis, surface its hidden obligations, inspect the polarity scheme.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build:share": "node build-share.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
